{% macro create(cancel_url) %}
<div class="form-actions">
    <button type="submit" class="btn btn-primary"><i class="fa fa-ok"></i> {{ 'sylius.create'|trans }}</button>
    <a href="{{ cancel_url|default(app.request.headers.get('referer')) }}" class="btn"><i class="fa fa-remove"></i> {{ 'sylius.cancel'|trans }}</a>
</div>
{% endmacro %}

{% macro update(cancel_url) %}
<div class="form-actions">
    <button type="submit" class="btn btn-primary"><i class="fa fa-save"></i> {{ 'sylius.save_changes'|trans }}</button>
    <a href="{{ cancel_url|default(app.request.headers.get('referer')) }}" class="btn"><i class="fa fa-remove"></i> {{ 'sylius.cancel'|trans }}</a>
</div>
{% endmacro %}
-
